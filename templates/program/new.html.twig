{% extends "base.html.twig" %} 

{% block title %}New Serie{% endblock %}

{% block body %}
    <h1>Nouvelle serie:</h1>
  
    {{form_start(form)}}
    {# {{ form_widget(form, {'attr': {'novalidate': 'novalidate'}}) }} #}
     {{ form_widget(form) }}
     <input type="submit" value="Ajouter une serie">
    {{ form_end(form)}}

    <a href="{{path('app_index')}}">
        Retour à l'acceuil
    </a>
{% endblock %}